import { ImageDecoderPipe } from './image-decoder.pipe';

describe('ImageDecoderPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageDecoderPipe();
    expect(pipe).toBeTruthy();
  });
});
